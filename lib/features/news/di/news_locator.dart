import 'package:get_it/get_it.dart';

/// Registers all News-feature dependencies.
///
/// Currently a placeholder — wire up cubit/repo/data-source here
/// once the News feature is implemented.
void setupNewsLocator(GetIt sl) {
  // TODO: Register news dependencies when feature is built.
  // Example:
  // sl.registerLazySingleton<NewsRemoteDataSource>(
  //   () => NewsRemoteDataSourceImpl(client: sl()),
  // );
  // sl.registerLazySingleton<NewsRepository>(
  //   () => NewsRepositoryImpl(remoteDataSource: sl()),
  // );
  // sl.registerFactory(
  //   () => NewsCubit(newsRepository: sl()),
  // );
}
